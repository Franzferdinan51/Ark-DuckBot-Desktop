<Window x:Class="ArkDuckBot.Views.SettingsModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Application Settings"
        Width="520" Height="600"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">

    <Border Background="{DynamicResource SurfaceAlt}"
            BorderBrush="{DynamicResource CardBorder}"
            BorderThickness="2"
            CornerRadius="12">
        <DockPanel Margin="20">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <DockPanel>
                    <TextBlock Text="Settings"
                               Foreground="White"
                               FontSize="22"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    <Button Content="✕"
                            HorizontalAlignment="Right"
                            Width="30" Height="30"
                            Style="{StaticResource GhostButton}"
                            Click="BtnClose_Click"
                            Cursor="Hand"
                            FontSize="18"
                            Foreground="{DynamicResource TextSubtle}"/>
                </DockPanel>
                <Separator Background="{DynamicResource CardBorder}" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Footer -->
            <Button DockPanel.Dock="Bottom"
                    Content="Close"
                    Width="100"
                    Height="35"
                    HorizontalAlignment="Right"
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnClose_Click"
                    Margin="0,15,0,0"/>

            <!-- Content -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource {x:Type ScrollViewer}}">
                <StackPanel>
                    <TextBlock Text="General" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,0,0,10"/>

                    <CheckBox x:Name="ChkAutoStart"
                               Content="Launch with Windows (Minimized)"
                               Margin="0,0,0,12"
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkStartMinimized"
                               Content="Start Minimized (Always)"
                               Margin="0,0,0,12"
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkAutoConnect"
                               Content="Auto-Connect to last server on startup"
                               Margin="0,0,0,12"
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Automatically reconnects to the last server and loads the map."/>

                    <TextBlock Text="Behavior" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>

                    <CheckBox x:Name="ChkCloseToTray"
                               Content="Minimize to Tray instead of closing"
                               Margin="0,0,0,12"
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkBackgroundTracking"
                               Content="Enable Background Player Tracking"
                               Margin="0,0,0,12"
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Keeps tracking players via BattleMetrics even when minimized."/>

                    <CheckBox x:Name="ChkHideConsole"
                               Content="Hide System Console (Declutter)"
                               Margin="0,0,0,12"
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Hides the bottom log/console panel to save map space."/>

                    <CheckBox x:Name="ChkAutoLoadShops"
                               Content="Auto load shops on connection"
                               Margin="0,0,0,12"
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Automatically starts polling shops and alerts when connected to a server."/>

                    <!-- DuckBot MCP Bridge Section -->
                    <TextBlock Text="DuckBot MCP Bridge" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,15,0,10"/>

                    <TextBlock Text="Host" Foreground="{DynamicResource TextSubtle}" FontSize="11" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtMcpHost"
                             Text="{Binding McpHost, Mode=TwoWay}"
                             Style="{StaticResource DarkTextBox}"
                             Height="32"
                             Margin="0,0,0,10"
                             TextChanged="OnMcpSettingChanged"/>

                    <TextBlock Text="Port" Foreground="{DynamicResource TextSubtle}" FontSize="11" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtMcpPort"
                             Text="{Binding McpPort, Mode=TwoWay}"
                             Style="{StaticResource DarkTextBox}"
                             Height="32"
                             Margin="0,0,0,10"
                             TextChanged="OnMcpSettingChanged"/>

                    <TextBlock Text="Shared Secret" Foreground="{DynamicResource TextSubtle}" FontSize="11" Margin="0,0,0,4"/>
                    <PasswordBox x:Name="TxtMcpSecret"
                                 Style="{StaticResource DarkPasswordBox}"
                                 Height="32"
                                 Margin="0,0,0,10"
                                 PasswordChanged="OnMcpSecretChanged"/>

                    <TextBlock Text="AI Provider" Foreground="{DynamicResource TextSubtle}" FontSize="11" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CmbAiProvider"
                              Style="{StaticResource DarkComboBox}"
                              Height="32"
                              Margin="0,0,0,10"
                              SelectionChanged="OnAiProviderChanged">
                        <ComboBoxItem Content="OpenRouter" Tag="openrouter"/>
                        <ComboBoxItem Content="Anthropic (Claude)" Tag="anthropic"/>
                        <ComboBoxItem Content="OpenAI (GPT-4)" Tag="openai"/>
                        <ComboBoxItem Content="Google Gemini" Tag="gemini"/>
                        <ComboBoxItem Content="LM Studio (Local)" Tag="lmstudio"/>
                        <ComboBoxItem Content="Ollama (Local)" Tag="ollama"/>
                    </ComboBox>

                    <TextBlock Text="Connection Status" Foreground="{DynamicResource TextSubtle}" FontSize="11" Margin="0,0,0,4"/>
                    <TextBlock x:Name="TxtMcpStatus"
                               Text="Not connected"
                               Foreground="#8090A0"
                               FontSize="12"
                               Margin="0,0,0,10"/>

                    <!-- Chat Section -->
                    <TextBlock Text="Chat" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Button Content="Modify Chat Alerts"
                                Style="{StaticResource PrimaryButton}"
                                Background="#3D5A80"
                                Height="36"
                                Padding="32,0"
                                Click="BtnModifyChatAlerts_Click"/>
                        <Button Content="Chat Commands"
                                Style="{StaticResource PrimaryButton}"
                                Background="#3D5A80"
                                Height="36"
                                Margin="12,0,0,0"
                                Padding="32,0"
                                Click="BtnChatCommands_Click"/>
                    </StackPanel>

                    <TextBlock Text="Steam Account" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    <TextBlock Text="Manage your linked Steam account in the main window."
                               Foreground="{DynamicResource TextSubtle}"
                               FontSize="12"
                               FontStyle="Italic"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>